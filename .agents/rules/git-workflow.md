# Git Workflow Rules

* **Branching:** Name branches according to concern (e.g., `feature/description` or `bugfix/description`).
* **Commits:** Write descriptive, conventional commit messages. Use `make commit MSG="..."` to enforce formatting and verification.
* **Pull Requests:** Provide a clean markdown summary in PR descriptions. Link relevant issues.
