# Styling Rules

* **Tech Stack:** Vanilla CSS for component-level styles. Avoid TailwindCSS unless explicitly requested by the user.
* **Aesthetics:** Use curated, premium, harmonious color palettes (prefer sleek dark modes, HSL tailored colors, clean gradients).
* **Animations:** Use subtle hover transitions and micro-animations for interactive elements.
