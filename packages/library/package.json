{
	"name": "@ai-learning-support/library",
	"version": "1.0.0",
	"private": true,
	"description": "A reusable library for embedding learning support into other applications.",
	"main": "dist/index.js",
	"module": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup src/index.ts --format esm --dts",
		"lint": "biome lint src",
		"format": "biome format src --write",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"devDependencies": {
		"@ai-learning-support/core": "workspace:*"
	}
}
