{
	"name": "@ai-learning-support/tsconfig",
	"version": "1.0.0",
	"private": true,
	"description": "Shared TypeScript configurations for ai-learning-support",
	"files": [
		"base.json",
		"nextjs.json"
	],
	"scripts": {
		"lint": "biome lint .",
		"format": "biome format . --write",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	}
}
