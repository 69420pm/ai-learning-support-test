{
	"name": "@ai-learning-support/tsconfig",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Shared TypeScript configurations for ai-learning-support",
	"files": [
		"base.json",
		"nextjs.json"
	],
	"scripts": {
		"lint": "biome lint .",
		"format": "biome format . --write",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --project=tsconfig"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.5"
	}
}
